import { useState } from "react";
import { Pencil } from "lucide-react";
import TwoFactorModal from "../TwoFactorModal";

const AccountSecurity = () => {
  const [twoStep, setTwoStep] = useState(true);
  const [showModal, setShowModal] = useState(false);

  return (
    <div className="max-w-5xl">

      {/* Section Title */}
      <h2 className="text-xl font-semibold text-black dark:text-white mb-6">
        Account & Security
      </h2>

      {/* User Row */}
      <div className="flex items-center justify-between mb-10">
        <div className="flex items-center gap-4">
          <div className="w-14 h-14 rounded-full bg-gradient-to-br from-red-600 to-red-800 flex items-center justify-center text-white text-xl font-semibold">
            J
          </div>

          <div>
            <h3 className="font-semibold text-black dark:text-white">
              John Doe
            </h3>
            <p className="text-sm text-gray-500">
              JohnDoeEmployee@gmail.com
            </p>
          </div>
        </div>

        <button className="px-5 py-1.5 rounded-full border border-[#2461E6] text-[#2461E6] text-sm font-semibold hover:bg-blue-50 transition">
          Manage
        </button>
      </div>

      {/* Two Step Card */}
      <div className="w-full rounded-2xl border border-[#EAECEF] dark:border-[#575757] p-6 flex items-center justify-between shadow-sm">

        <div>
          <h3 className="text-xl font-semibold text-black dark:text-white">
            Two-step verification
          </h3>
          <p className="text-sm text-gray-500 mt-1">
            We recommend requiring a verification code in addition to your password
          </p>
        </div>

        <div className="flex items-center gap-6">

          {/* Toggle */}
          <div
            onClick={() => setTwoStep(prev => !prev)}
            className={`w-14 h-7 flex items-center rounded-full p-1 cursor-pointer transition
              ${twoStep ? "bg-[#2461E6]" : "bg-gray-300"}
            `}
          >
            <div
              className={`w-5 h-5 bg-white rounded-full shadow-md transform transition
                ${twoStep ? "translate-x-7" : ""}
              `}
            />
          </div>

          {/* Label */}
          <span className="text-sm text-gray-600 dark:text-gray-300">
            Two-step verification
          </span>

          {/* Edit Button */}
          <button 
            onClick={() => setShowModal(true)}
            className="px-4 py-1.5 rounded-full bg-[#2461E6] text-white text-sm font-semibold flex items-center gap-2 hover:opacity-90 transition"
          >
            <Pencil size={16} />
            edit
          </button>
        </div>
      </div>
      {showModal && <TwoFactorModal onClose={() => setShowModal(false)} />}
    </div>
  );
};

export default AccountSecurity;
